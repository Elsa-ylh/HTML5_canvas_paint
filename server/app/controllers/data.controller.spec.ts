// tslint:disable:no-any
const HTTP_STATUS_OK = 200;

describe('Data Controller', () => {
    // const baseMessageErreur = { title: 'Error', body: '' } as Message;
   
    /*
    let dataService: Stubbed<DatabaseService>;
    let app: Express.Application;

    beforeEach(async () => {
        const [container, sandbox] = await testingContainer();
        container.rebind(TYPES.DatabaseService).toConstantValue({
            // getImageLabals: sandbox.stub().resolves(dataRetour),
            getImage: sandbox.stub(),
        });
        dataService = container.get(TYPES.DatabaseService);
        app = container.get<Application>(TYPES.Application).app;
    });

    it('should return time from', async () => {
        return supertest(app)
            .get('/api/data')
            .expect(HTTP_STATUS_OK)
            .then((reponse: any) => {
                // expect(reponse).to.deep.equal(dataAll);
            });
    });*/
});
