import { CreateNewDrawingDirective } from './create-new-drawing.directive';

describe('CreateNewDrawingDirective', () => {
    it('should create an instance', () => {
        const directive = new CreateNewDrawingDirective();
        expect(directive).toBeTruthy();
    });
});
