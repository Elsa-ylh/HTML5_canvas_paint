import { ContinueDrawingDirective } from './continue-drawing.directive';

describe('ContinueDrawingDirective', () => {
  it('should create an instance', () => {
    const directive = new ContinueDrawingDirective();
    expect(directive).toBeTruthy();
  });
});
