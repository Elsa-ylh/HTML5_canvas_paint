import { OpenDocumentationDirective } from './open-documentation.directive';

describe('OpenDocumentationDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenDocumentationDirective();
    expect(directive).toBeTruthy();
  });
});
