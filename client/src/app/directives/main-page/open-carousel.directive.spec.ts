import { OpenCarouselDirective } from './open-carousel.directive';

describe('OpenCarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenCarouselDirective();
    expect(directive).toBeTruthy();
  });
});
