export const DEFAULT_WIDTH = window.innerWidth / 2;
export const DEFAULT_HEIGHT = window.innerHeight / 2;
export const sizeSidebar = 200;
export const minSizeWindow = 500;
export const minSizeCanvas = 250;
