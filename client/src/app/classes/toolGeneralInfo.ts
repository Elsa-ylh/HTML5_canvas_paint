import { SubToolselected } from './sub-tool-selected';

export class ToolGeneralInfo {
    primaryColor?: string;
    secondaryColor?: string;
    lineWidth?: number;
    shiftPressed?: boolean;
    selectSubTool?: SubToolselected;
    canvasSeclected?: boolean;
}
